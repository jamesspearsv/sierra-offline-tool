services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
    restart: unless-stopped
    volumes:
      - app-data:/app/data
volumes:
  app-data:
